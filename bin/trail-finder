#!/usr/bin/env ruby 

# shabang line telling the operating system how to interpret the file using ruby
# and given permisions to interpret the file using the - chmod +x trail-finder - 
# command in terminal 
                       
#puts "Hello World!"   # test line of code #

require_relative  '../lib/trail_finder.rb'   # Program entry point, anything that needs my environment can load it with this one line call.

TrailFinder::CliController.new.call          # Creates a new instance of CliController and calls the call method on that instance which starts the program running.

